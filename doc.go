// Command omada-duckdns-updater fetches WAN IP addresses from a TP-Link Omada
// SDN Controller and updates DuckDNS domains on a configurable schedule.
//
// The application exposes a built-in web UI for configuration and provides a
// live status dashboard. On Windows it can run as a native service; on Linux it
// is typically managed with systemd or Docker.
package main
