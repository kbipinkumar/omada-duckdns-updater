# Omada SDN Controller URL (e.g., https://192.168.1.1:8043)
OMADA_URL=https://<your-omada-controller-ip>:<port>

# Optional: restrict OMADA_URL to specific hostnames/IPs (comma-separated).
# When unset, any valid HTTPS host is accepted. Recommended for hardened deployments.
# OMADA_ALLOWED_HOSTS=192.168.1.1,omada.local

# Omada API Credentials (created in Omada Settings -> Global -> API)
OMADA_CLIENT_ID=your-omada-client-id
OMADA_CLIENT_SECRET=your-omada-client-secret

# Omada Cloud ID (used if your controller is linked to TP-Link Cloud, otherwise leave blank or refer to docs)
OMADA_OMADAC_ID=your-omadac-id

# Your Omada Site ID (can be fetched automatically from the Web UI)
OMADA_SITE_ID=your-site-id

# DuckDNS Configuration
DUCKDNS_TOKEN=your-duckdns-token
DUCKDNS_DOMAIN=yourdomain1.duckdns.org,yourdomain2.duckdns.org

# How often the updater checks for IP changes (in minutes)
UPDATE_INTERVAL=5

# Web UI Basic Authentication (Optional)
# If set, the Web UI will require these credentials to access the dashboard.
# When saved via the Web UI, the password is bcrypt-hashed. API tokens are encrypted
# at rest with a per-install key (.encryption-key in the data directory).
# updater.conf is written with mode 0600 on Unix.
# Set the password through the Web UI instead of editing this file directly.
WEB_USERNAME=admin
WEB_PASSWORD=

# Optional environment variable overrides (useful for Docker/GitOps).
# When set, these take precedence over values in this file at runtime:
#   OMADA_CLIENT_SECRET
#   DUCKDNS_TOKEN
#   WEB_PASSWORD  (plaintext in env only; not written back to updater.conf)
