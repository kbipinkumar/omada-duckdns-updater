name: "omada-duckdns-updater CodeQL"
query-filters:
  # Legacy salted SHA-256 verification lives in password_legacy.go for one-release
  # migration only. All new passwords use bcrypt in crypto.go.
  - exclude:
      id: go/weak-sensitive-data-hashing
